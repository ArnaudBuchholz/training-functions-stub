// Ease the presenter job by offering prepared lines of code to copy & paste

it("is accessible through sinon.spy");
it("accepts no parameter");
it("accepts a function parameter");
it("rejects any other parameter");
it("returns a function");
it("returns a function that behaves like the parameter");
it("returns a function that does nothing when no parameter was specified");

describe("US2", function () {

    describe("it exposes the property \"called\" the indicates if the function was called", function () {
        it("was never called");
        it("was called once");
        var COUNT = 10;
        it("was called " + COUNT + " times - synchronously");
    });

    describe("it exposes the property \"callCount\" that indicates how often the function was called", function () {
    });

});
